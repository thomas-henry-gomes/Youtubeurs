<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.

    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <!-- Custom theme. -->
    <style name="MyCustomTheme" parent="android:Theme.Holo.Light">
		<item name="android:textStyle">italic</item>
		<item name="android:typeface">sans</item>
		<item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="ptrHeaderStyle">@style/Widget.Custom.PtrHeader</item>
	</style>
	<!-- Button Custom theme. -->
    <style name="MyButtonCustomTheme" parent="android:Theme.Holo.Light">
		<item name="android:textStyle">italic</item>
		<item name="android:typeface">sans</item>
		<item name="android:textColor">#FFFFFF</item>
	</style>
    <!--  -->
    <style name="Widget.Custom.PtrHeader" parent="android:Widget">
        <item name="ptrProgressBarColor">#A4000A</item>
        <item name="ptrPullText">Tirez pour rafraîchir ...</item>
        <item name="ptrRefreshingText">Actualisation en cours ...</item>
    </style>
</resources>
